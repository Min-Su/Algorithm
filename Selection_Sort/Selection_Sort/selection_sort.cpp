#include<stdio.h>
#include<stdlib.h>

#define SWAP(x, y, t) ( (t)=(x), (x)=(y), (y)=(t) )

void selection_sort(int list[], int n);

int main()
{
	return 0;
}

void selection_sort(int list[], int n)
{

}